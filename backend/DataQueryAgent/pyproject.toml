[project]
name = "data-query-agent"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]==0.124.4",
    "uvicorn==0.38.0",
    "aiokafka==0.12.0",
    "sqlalchemy>=2.0",
    "psycopg2-binary>=2.9",
    "civis-obs",
]

[tool.uv.sources]
civis-obs = { workspace = true }
